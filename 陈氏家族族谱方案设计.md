# 陈氏家族族谱系统设计方案

## 一、项目概述

### 1.1 项目背景
陈氏家族族谱系统是一个用于记录、管理和展示家族成员信息及血脉关系的综合性平台。系统支持多用户协作，能够清晰地展示家族成员的代际关系，方便家族成员查阅和更新族谱信息。

### 1.2 核心目标
- 建立完整的家族成员档案库
- 清晰展示家族血脉脉络关系
- 支持多用户协作管理
- 提供便捷的查询和浏览功能
- 传承家族历史与文化

---

## 二、技术架构

### 2.1 整体架构
```
┌─────────────────────────────────────────────────────────┐
│                      前端层 (Frontend)                    │
│                   Vue 3 + Element Plus                    │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                      后端层 (Backend)                     │
│                   Node.js + Express                       │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                      数据层 (Database)                    │
│                     MySQL / MongoDB                       │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 | 渐进式JavaScript框架，组件化开发 |
| UI组件库 | Element Plus | 企业级UI组件库 |
| 状态管理 | Pinia | Vue 3官方推荐的状态管理库 |
| 图形可视化 | D3.js / GoJS | 用于绘制家族树形图 |
| 后端框架 | Node.js + Express | 高性能Web服务框架 |
| 数据库 | MySQL | 关系型数据库，适合复杂关系查询 |
| 认证 | JWT | JSON Web Token身份认证 |
| 文件存储 | 本地存储 / OSS | 照片等文件存储 |

---

## 三、功能模块设计

### 3.1 用户管理模块

#### 3.1.1 功能列表
| 功能 | 说明 |
|------|------|
| 用户注册 | 新用户注册账号，需审核通过 |
| 用户登录 | 账号密码登录，支持记住登录状态 |
| 密码找回 | 通过邮箱或手机号找回密码 |
| 个人信息 | 修改个人基本信息 |
| 权限管理 | 管理员可设置用户权限级别 |

#### 3.1.2 用户角色
- **管理员**：拥有所有权限，可管理用户和家族成员
- **编辑者**：可添加、编辑家族成员信息
- **查看者**：仅可查看家族成员信息

### 3.2 家族成员管理模块

#### 3.2.1 成员信息字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | String | 是 | 唯一标识符 |
| name | String | 是 | 姓名 |
| gender | Enum | 是 | 性别（男/女） |
| photo | String | 否 | 照片URL |
| birthDate | Date | 是 | 出生日期 |
| deathDate | Date | 否 | 离世日期（在世则为空） |
| birthPlace | String | 否 | 出生地点 |
| residence | String | 否 | 居住地点 |
| spouseName | String | 否 | 配偶姓名 |
| spouseId | String | 否 | 配偶成员ID（关联） |
| fatherId | String | 否 | 父亲成员ID |
| motherId | String | 否 | 母亲成员ID |
| generation | Number | 是 | 第几代（从始祖为1） |
| orderInGeneration | Number | 是 | 同代中的排序 |
| education | String | 否 | 学历/教育背景 |
| occupation | String | 否 | 职业 |
| contribution | Text | 否 | 社会贡献/主要事迹 |
| biography | Text | 否 | 个人简介 |
| createdAt | DateTime | 是 | 创建时间 |
| updatedAt | DateTime | 是 | 更新时间 |
| createdBy | String | 是 | 创建者ID |
| updatedBy | String | 是 | 最后更新者ID |

#### 3.2.2 功能列表
| 功能 | 说明 |
|------|------|
| 添加成员 | 添加新的家族成员，需指定父/母关系 |
| 编辑成员 | 修改成员基本信息 |
| 删除成员 | 删除成员（需确认无后代） |
| 查看详情 | 查看成员完整信息 |
| 上传照片 | 上传成员照片 |
| 批量导入 | 支持Excel批量导入成员数据 |
| 导出族谱 | 导出族谱为PDF或Excel |

### 3.3 家族脉络展示模块

#### 3.3.1 展示方式
1. **树形图视图**
   - 以始祖为根节点，向下展开各代
   - 支持缩放、拖拽、节点展开/折叠
   - 点击节点查看详细信息

2. **代际列表视图**
   - 按代分组展示，每代一个区块
   - 清晰展示同代所有成员
   - 支持快速定位到某一代

3. **时间轴视图**
   - 按出生日期排序展示
   - 可视化家族发展历程

4. **关系图谱视图**
   - 展示成员间的婚姻关系
   - 突出显示血缘关系线

#### 3.3.2 交互功能
- 搜索成员（按姓名、代数等）
- 筛选（按性别、在世/已故等）
- 定位到指定成员
- 显示直系血亲路径
- 高亮显示选中成员的亲属关系

### 3.4 统计分析模块

| 统计项 | 说明 |
|--------|------|
| 总人数 | 家族成员总数 |
| 在世人数 | 当前在世成员数 |
| 代数统计 | 各代人数分布 |
| 性别比例 | 男女比例统计 |
| 地域分布 | 居住地分布统计 |
| 年龄分布 | 各年龄段人数统计 |

---

## 四、数据库设计

### 4.1 ER图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    User     │       │   Member    │       │    Photo    │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │       │ id          │       │ id          │
│ username    │       │ name        │       │ memberId    │
│ password    │       │ gender      │       │ url         │
│ email       │       │ birthDate   │       │ description │
│ phone       │       │ deathDate   │       │ uploadTime  │
│ role        │       │ generation  │       └─────────────┘
│ status      │       │ fatherId    │
│ createdAt   │       │ motherId    │
└─────────────┘       │ spouseId    │
                      │ ...         │
                      └─────────────┘
```

### 4.2 数据表设计

#### 4.2.1 用户表 (users)
```sql
CREATE TABLE users (
    id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    role ENUM('admin', 'editor', 'viewer') DEFAULT 'viewer',
    status ENUM('pending', 'active', 'disabled') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 4.2.2 家族成员表 (members)
```sql
CREATE TABLE members (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    gender ENUM('male', 'female') NOT NULL,
    photo_url VARCHAR(255),
    birth_date DATE NOT NULL,
    death_date DATE,
    birth_place VARCHAR(100),
    residence VARCHAR(100),
    spouse_name VARCHAR(50),
    spouse_id VARCHAR(36),
    father_id VARCHAR(36),
    mother_id VARCHAR(36),
    generation INT NOT NULL,
    order_in_generation INT DEFAULT 0,
    education VARCHAR(100),
    occupation VARCHAR(100),
    contribution TEXT,
    biography TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by VARCHAR(36),
    updated_by VARCHAR(36),
    
    FOREIGN KEY (father_id) REFERENCES members(id) ON DELETE SET NULL,
    FOREIGN KEY (mother_id) REFERENCES members(id) ON DELETE SET NULL,
    FOREIGN KEY (spouse_id) REFERENCES members(id) ON DELETE SET NULL,
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (updated_by) REFERENCES users(id)
);
```

#### 4.2.3 照片表 (photos)
```sql
CREATE TABLE photos (
    id VARCHAR(36) PRIMARY KEY,
    member_id VARCHAR(36) NOT NULL,
    url VARCHAR(255) NOT NULL,
    description VARCHAR(255),
    upload_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (member_id) REFERENCES members(id) ON DELETE CASCADE
);
```

---

## 五、API接口设计

### 5.1 用户相关接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/logout | 用户登出 |
| GET | /api/auth/profile | 获取当前用户信息 |
| PUT | /api/auth/profile | 更新用户信息 |
| PUT | /api/auth/password | 修改密码 |

### 5.2 成员管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/members | 获取成员列表（支持分页、筛选） |
| GET | /api/members/:id | 获取成员详情 |
| POST | /api/members | 添加新成员 |
| PUT | /api/members/:id | 更新成员信息 |
| DELETE | /api/members/:id | 删除成员 |
| GET | /api/members/:id/ancestors | 获取祖先链 |
| GET | /api/members/:id/descendants | 获取后代列表 |
| GET | /api/members/:id/relatives | 获取亲属关系 |
| POST | /api/members/:id/photo | 上传照片 |

### 5.3 族谱展示接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/tree | 获取完整家族树数据 |
| GET | /api/tree/generation/:gen | 获取指定代成员 |
| GET | /api/tree/stats | 获取统计数据 |
| GET | /api/tree/search | 搜索成员 |

---

## 六、前端页面设计

### 6.1 页面结构

```
├── 登录页 (/login)
├── 注册页 (/register)
├── 主页 (/)
│   ├── 家族树视图
│   ├── 代际列表视图
│   └── 统计概览
├── 成员管理 (/members)
│   ├── 成员列表
│   ├── 添加成员
│   └── 编辑成员 (/members/:id/edit)
├── 成员详情 (/members/:id)
├── 用户管理 (/admin/users) [管理员]
└── 个人中心 (/profile)
```

### 6.2 核心页面设计

#### 6.2.1 家族树页面
```
┌────────────────────────────────────────────────────────────┐
│  [Logo] 陈氏家族族谱          [搜索框]    [用户头像▼]        │
├────────────────────────────────────────────────────────────┤
│  [树形图] [代际列表] [时间轴] [统计]                         │
├────────────────────────────────────────────────────────────┤
│                                                            │
│                         第1代                               │
│                        [始祖]                               │
│                           │                                │
│                         第2代                               │
│                    ┌─────┴─────┐                           │
│                  [长子]      [次子]                         │
│                    │           │                           │
│                         第3代                               │
│                 ┌──┴──┐    ┌──┴──┐                         │
│               [孙1] [孙2] [孙3] [孙4]                       │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 6.2.2 成员详情页面
```
┌────────────────────────────────────────────────────────────┐
│  [返回]  陈某某 - 第三代                                    │
├────────────────────────────────────────────────────────────┤
│  ┌──────────┐  姓名：陈某某                                │
│  │          │  性别：男                                    │
│  │   照片   │  出生日期：1950年3月15日                      │
│  │          │  离世日期：2020年8月20日                      │
│  └──────────┘  居住地：广东省广州市                         │
│                配偶：李某某                                 │
│                父亲：陈父名                                 │
│                母亲：陈母名                                 │
├────────────────────────────────────────────────────────────┤
│  教育背景：大学本科                                         │
│  职业：教师                                                │
│  社会贡献：从事教育工作30年，培养众多优秀学子...             │
│  个人简介：...                                             │
├────────────────────────────────────────────────────────────┤
│  [编辑] [删除]                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 七、项目目录结构

### 7.1 前端项目结构
```
frontend/
├── public/
│   └── index.html
├── src/
│   ├── api/                 # API接口封装
│   │   ├── auth.js
│   │   └── member.js
│   ├── assets/              # 静态资源
│   │   ├── images/
│   │   └── styles/
│   ├── components/          # 公共组件
│   │   ├── FamilyTree.vue   # 家族树组件
│   │   ├── MemberCard.vue   # 成员卡片
│   │   └── GenerationList.vue
│   ├── views/               # 页面视图
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   ├── Home.vue
│   │   ├── MemberList.vue
│   │   ├── MemberDetail.vue
│   │   └── Profile.vue
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── store/               # 状态管理
│   │   ├── auth.js
│   │   └── member.js
│   ├── utils/               # 工具函数
│   │   └── request.js
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

### 7.2 后端项目结构
```
backend/
├── config/
│   ├── database.js          # 数据库配置
│   └── jwt.js               # JWT配置
├── controllers/
│   ├── authController.js    # 认证控制器
│   └── memberController.js  # 成员控制器
├── middleware/
│   ├── auth.js              # 认证中间件
│   └── upload.js            # 文件上传中间件
├── models/
│   ├── User.js              # 用户模型
│   └── Member.js            # 成员模型
├── routes/
│   ├── auth.js              # 认证路由
│   └── member.js            # 成员路由
├── services/
│   ├── authService.js
│   └── memberService.js
├── uploads/                 # 上传文件目录
├── utils/
│   └── helpers.js
├── app.js
├── server.js
└── package.json
```

---

## 八、开发计划

### 8.1 开发阶段

| 阶段 | 内容 | 预计时间 |
|------|------|----------|
| 第一阶段 | 项目初始化、数据库设计、基础框架搭建 | 3天 |
| 第二阶段 | 用户认证模块（注册、登录、权限） | 3天 |
| 第三阶段 | 成员管理模块（增删改查） | 5天 |
| 第四阶段 | 家族树可视化展示 | 5天 |
| 第五阶段 | 统计分析、导出功能 | 3天 |
| 第六阶段 | 测试、优化、部署 | 3天 |

### 8.2 里程碑
- **M1**：完成基础框架，用户可注册登录
- **M2**：完成成员管理，可添加编辑成员
- **M3**：完成家族树展示，可视化血脉关系
- **M4**：系统完整可用，准备部署上线

---

## 九、部署方案

### 9.1 服务器要求
- 操作系统：Linux (Ubuntu/CentOS)
- Node.js >= 16.x
- MySQL >= 8.0
- Nginx（反向代理）

### 9.2 部署架构
```
用户请求 → Nginx → Node.js应用 → MySQL数据库
                ↓
            静态文件（前端）
```

### 9.3 部署步骤
1. 安装Node.js和MySQL
2. 导入数据库结构和初始数据
3. 部署后端服务（使用PM2管理进程）
4. 构建前端并部署到Nginx
5. 配置Nginx反向代理
6. 配置SSL证书（HTTPS）

---

## 十、安全考虑

### 10.1 安全措施
- 密码加密存储（bcrypt）
- JWT Token认证
- API接口权限验证
- SQL注入防护
- XSS攻击防护
- CSRF防护
- 文件上传类型和大小限制

### 10.2 数据备份
- 定期数据库备份
- 上传文件备份
- 备份数据异地存储

---

## 十一、扩展功能（未来规划）

| 功能 | 说明 |
|------|------|
| 移动端适配 | 响应式设计或开发独立APP |
| 家族大事记 | 记录家族重要事件 |
| 家族相册 | 共享家族照片 |
| 家族论坛 | 家族成员交流讨论 |
| 家族活动 | 组织家族聚会等活动 |
| 数据导入导出 | 支持更多格式（GEDCOM等） |
| 多家族支持 | 支持管理多个家族族谱 |

---

## 十二、总结

本设计方案从技术架构、功能模块、数据库设计、API接口、前端页面等多个维度进行了详细规划。系统采用前后端分离架构，使用Vue 3和Node.js作为主要技术栈，MySQL作为数据存储。核心功能包括用户管理、家族成员管理、家族脉络可视化展示等，能够满足家族族谱管理的基本需求，并预留了扩展空间。

通过清晰的代际关系展示和直观的树形图可视化，用户可以轻松了解家族成员的血脉关系，传承家族历史与文化。
