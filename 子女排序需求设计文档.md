# 子女排序需求设计文档

## 1. 需求概述

在家族树展示中，为每个有子女的节点内的子女按出生日期排序，并在每个子女节点的**右上角**显示排位标识，格式为"长子"、"二女"、"三子"、"四女"等。

**核心规则**：排位仅在**同一对父母的子女**之间计算，不同父母家庭的排位互相独立。

---

## 2. 排位命名规则

### 2.1 基本规则

- 排位基于 `birth_date` 升序排序，出生最早者为"长"
- 男性用"子"，女性用"女"
- 第1位：长（长子 / 长女）
- 第2位起：二、三、四、五、六、七、八、九、十、十一、十二……

### 2.2 命名示例

| 排位 | 男性 | 女性 |
|------|------|------|
| 1    | 长子 | 长女 |
| 2    | 二子 | 二女 |
| 3    | 三子 | 三女 |
| 4    | 四子 | 四女 |
| ...  | N子  | N女  |

### 2.3 完整示例

假设某对父母有5个子女，按出生日期排序后：

| 顺序 | 姓名 | 性别 | 排位标识 |
|------|------|------|----------|
| 1    | 陈大 | male | 长子     |
| 2    | 陈二 | female | 二女   |
| 3    | 陈三 | male | 三子     |
| 4    | 陈四 | female | 四女   |
| 5    | 陈五 | male | 五子     |

---

## 3. 排位范围定义

### 3.1 父母对的确定

排位的分母是**同一对父母**的子女集合。父母对通过以下逻辑确定：

- 子女的 `father_id` 相同 且 `mother_id` 相同 → 同一对父母
- 仅 `father_id` 相同（单亲父亲）→ 同一父亲的子女
- 仅 `mother_id` 相同（单亲母亲）→ 同一母亲的子女

### 3.2 当前树结构适配

当前后端 `treeController.js` 构建树时，子女挂载逻辑为：
- 有 `father_id` → 挂在父亲节点下
- 仅 `mother_id` → 挂在母亲节点下

**排位计算应与树结构的 children 数组一致**：对某个节点的 `children` 数组中的全部子女，按 `birth_date` 排序后计算排位。

### 3.3 同父异母 / 同母异父场景

当一位父亲与不同母亲有子女时，这些子女的 `father_id` 相同，会被挂到同一个父亲节点的 `children` 下。排位计算覆盖全部 children，不区分子女来自哪位母亲。

> 如需区分"同父同母"与"同父异母"的排位，需额外需求定义。当前版本不做区分。

---

## 4. 数据层设计

### 4.1 数据库

**无需新增列**。

排位完全由运行时计算（`birth_date` + `gender` + 同父母子女排序），不在数据库持久化。理由：
- 排位是派生数据，新增/删除/修改成员时自动变化
- 避免数据不一致问题

### 4.2 后端 API 改动

在后端 `treeController.js` 的 `getFamilyTree` 函数中，构建完树结构后，遍历每个节点的 `children` 数组，为每个子女计算并注入 `sibling_order` 字段。

#### 改动位置

文件：`backend/controllers/treeController.js`

#### 新增工具函数

```javascript
/**
 * 中文数字映射
 */
const CN_NUMBERS = ['长', '二', '三', '四', '五', '六', '七', '八', '九', '十',
  '十一', '十二', '十三', '十四', '十五', '十六', '十七', '十八', '十九', '二十'];

/**
 * 生成子女排位标识
 * @param {number} order - 排位序号（从0开始）
 * @param {string} gender - 'male' | 'female'
 * @returns {string} 如"长子"、"二女"
 */
function getSiblingOrderLabel(order, gender) {
  const cnNum = CN_NUMBERS[order] || `${order + 1}`;
  const suffix = gender === 'female' ? '女' : '子';
  return `${cnNum}${suffix}`;
}

/**
 * 为节点的children计算排位
 * 在同一children数组内按birth_date排序后分配sibling_order和sibling_order_label
 */
function assignSiblingOrders(node) {
  if (!node.children || node.children.length === 0) return;

  // 按出生日期排序
  node.children.sort((a, b) => {
    const dateA = a.birth_date || '9999-12-31';
    const dateB = b.birth_date || '9999-12-31';
    return dateA.localeCompare(dateB);
  });

  // 分配排位
  node.children.forEach((child, index) => {
    child.sibling_order = index + 1;
    child.sibling_order_label = getSiblingOrderLabel(index, child.gender);
  });

  // 递归处理后代
  node.children.forEach(child => assignSiblingOrders(child));
}
```

#### 调用时机

在 `getFamilyTree` 函数中，`res.json()` 返回前，对树的根节点依次调用：

```javascript
tree.forEach(rootNode => assignSiblingOrders(rootNode));
```

#### API 返回数据结构变化

每个子女节点新增两个字段：

```json
{
  "id": "xxx",
  "name": "陈大",
  "gender": "male",
  "birth_date": "1990-01-01",
  "sibling_order": 1,
  "sibling_order_label": "长子",
  "children": [],
  "spouse": null
}
```

| 新增字段 | 类型 | 说明 |
|---------|------|------|
| `sibling_order` | Number | 在同父母子女中的排位序号（1起） |
| `sibling_order_label` | String | 中文排位标识，如"长子"、"二女" |

---

## 5. 前端展示设计

### 5.1 树节点（TreeNode.vue）改动

在主节点和配偶节点的右上角显示排位标识。

#### 5.1.1 位置：右上角

使用 CSS `position: absolute` 将标识定位到节点卡片的右上角：

```
┌──────────────┐
│         长子 │  ← 右上角标识
│   [头像]     │
│   陈大       │
│  第3代       │
└──────────────┘
```

#### 5.1.2 模板改动

主节点区域新增排位标识：

```html
<div class="node-content" :class="nodeGenderClass" @click="$emit('select', node)">
  <!-- 排位标识：仅在非根节点且有排位数据时显示 -->
  <span v-if="node.sibling_order_label" class="sibling-order-badge">
    {{ node.sibling_order_label }}
  </span>
  <el-avatar :size="50" :src="photoUrl" icon="UserFilled" />
  <div class="node-info">
    <span class="name">{{ node.name }}</span>
    <span class="generation">第{{ node.generation }}代</span>
  </div>
</div>
```

配偶节点**不显示**排位标识（配偶的排位属于其原生家庭，与当前家庭无关）。

#### 5.1.3 样式设计

```css
.node-content {
  position: relative; /* 已有，作为定位锚点 */
}

.sibling-order-badge {
  position: absolute;
  top: 2px;
  right: 2px;
  font-size: 10px;
  padding: 1px 4px;
  border-radius: 4px;
  line-height: 1.4;
  white-space: nowrap;
  z-index: 3;
}

/* 男性排位标识 - 蓝色系 */
.male-node .sibling-order-badge {
  background: rgba(64, 158, 255, 0.15);
  color: #409eff;
  border: 1px solid rgba(64, 158, 255, 0.3);
}

/* 女性排位标识 - 红色系 */
.female-node .sibling-order-badge {
  background: rgba(245, 108, 108, 0.15);
  color: #f56c6c;
  border: 1px solid rgba(245, 108, 108, 0.3);
}
```

### 5.2 成员详情抽屉（FamilyTree.vue）

在树页面右侧抽屉的 `el-descriptions` 中可选展示排位信息：

```html
<el-descriptions-item label="排位" v-if="selectedMember.sibling_order_label">
  {{ selectedMember.sibling_order_label }}
</el-descriptions-item>
```

### 5.3 成员详情页 / 成员列表

暂不改动。排位标识仅在家族树视图中展示，避免脱离家族上下文时产生歧义（如"长子"在不同家庭中含义不同）。

---

## 6. 边界场景处理

### 6.1 出生日期缺失

| 场景 | 处理方式 |
|------|---------|
| 所有子女都有 `birth_date` | 正常按日期排序 |
| 部分子女缺少 `birth_date` | 有日期的排前面，无日期的排后面（日期默认值 `9999-12-31`），无日期子女之间保持原有顺序 |
| 全部子女都缺 `birth_date` | 按 `order_in_generation` 字段排序；若该字段也为0，按数据库原始顺序 |

增强版排序逻辑：

```javascript
node.children.sort((a, b) => {
  // 优先按 birth_date
  const dateA = a.birth_date || '';
  const dateB = b.birth_date || '';
  if (dateA && dateB) return dateA.localeCompare(dateB);
  if (dateA && !dateB) return -1;  // 有日期排前
  if (!dateA && dateB) return 1;
  // 都没日期，按 order_in_generation
  const orderA = a.order_in_generation || 0;
  const orderB = b.order_in_generation || 0;
  if (orderA !== orderB) return orderA - orderB;
  return 0; // 保持原序
});
```

### 6.2 出生日期相同

同一天出生（双胞胎/龙凤胎）时，按 `order_in_generation` 辅助排序；若也相同，保持数据库原始顺序。排位序号仍然递增（不并列）。

### 6.3 独生子女

只有一个子女时，仍显示排位标识（"长子"或"长女"）。

### 6.4 根节点（始祖）

根节点没有父母，`sibling_order_label` 为空，不显示排位标识。

### 6.5 配偶的排位

配偶在其原生家庭中有自己的排位，但在当前树视图中，配偶作为附属节点显示，**不展示排位标识**。如需查看配偶在其原生家庭中的排位，需通过详情页查看。

---

## 7. 改动文件清单

| 文件 | 改动内容 |
|------|---------|
| `backend/controllers/treeController.js` | 新增 `getSiblingOrderLabel`、`assignSiblingOrders` 函数；在返回树数据前调用排位计算 |
| `frontend/src/components/TreeNode.vue` | 模板新增 `sibling-order-badge` 元素；样式新增排位标识的定位与配色 |

可选改动：
| 文件 | 改动内容 |
|------|---------|
| `frontend/src/views/FamilyTree.vue` | 抽屉详情中展示排位信息（低优先级） |

---

## 8. 测试要点

1. **基本排序**：同父母多子女，按出生日期排序，排位标识正确
2. **性别混合**：男女混合子女家庭，标识中"子/女"与性别对应
3. **独生子女**：单子女家庭显示"长子"或"长女"
4. **日期缺失**：部分子女无出生日期，有日期的排前，排位仍连续递增
5. **多家庭独立**：不同父母的子女排位互不影响
6. **根节点**：根节点不显示排位标识
7. **配偶节点**：配偶节点不显示排位标识
8. **中文数字**：验证超过10个子女时的中文数字正确性（十一子、十二女等）

---

## 9. 示意图

```
              [始祖] ─── [始祖母]
                  │
     ┌────────────┼────────────┐
     │            │            │
  [陈大]      [陈二]       [陈三]
  长子♂       二女♀        三子♂
     │            │            
  ┌──┴──┐     ┌──┴──┐        
  │     │     │     │        
[陈四] [陈五] [陈六] [陈七]
长子♂  二女♀  长女♀  二子♂
```

注意：每个家庭单元内的排位独立计算。陈大的子女（陈四、陈五）排位为长子、二女；陈二的子女（陈六、陈七）排位为长女、二子——两者互不影响。
