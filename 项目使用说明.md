# 陈氏家族族谱系统 - 项目使用说明

## 一、系统状态

| 服务 | 状态 | 地址 |
|------|------|------|
| 后端服务 | ✅ 运行中 | http://localhost:3000 |
| 前端服务 | ✅ 运行中 | http://localhost:5173 |
| 数据库 | ✅ 已初始化 | MySQL - family_tree |

---

## 二、默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |

⚠️ **请登录后立即修改密码！**

---

## 三、已实现功能

### 后端 API

| 功能模块 | 说明 |
|----------|------|
| 用户注册/登录 | JWT Token 认证 |
| 成员管理 | 增删改查、亲属关系 |
| 家族树数据 | 树形结构数据获取 |
| 统计数据 | 人数、代数、性别统计 |
| 照片上传 | 支持头像上传 |
| 用户管理 | 管理员审核用户、设置权限 |

### 前端页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 登录页 | /login | 用户登录 |
| 注册页 | /register | 用户注册（需审核） |
| 家族树 | / | 树形图 + 代际列表视图 |
| 成员列表 | /members | 成员管理、筛选搜索 |
| 成员详情 | /members/:id | 查看成员完整信息 |
| 添加/编辑成员 | /members/add, /members/:id/edit | 编辑权限用户可用 |
| 个人中心 | /profile | 修改个人信息、密码 |
| 用户管理 | /admin/users | 管理员专用 |

---

## 四、访问方式

打开浏览器访问: **http://localhost:5173**

---

## 五、项目结构

```
D:\陈氏家族族谱_dev\
├── backend/                    # 后端 Node.js + Express
│   ├── config/                 # 配置文件
│   │   ├── database.js         # 数据库连接配置
│   │   └── jwt.js              # JWT配置
│   ├── controllers/            # 控制器
│   │   ├── authController.js   # 认证控制器
│   │   ├── memberController.js # 成员控制器
│   │   └── treeController.js   # 家族树控制器
│   ├── models/                 # 数据模型
│   │   ├── User.js             # 用户模型
│   │   └── Member.js           # 成员模型
│   ├── routes/                 # 路由
│   │   ├── auth.js             # 认证路由
│   │   ├── member.js           # 成员路由
│   │   └── tree.js             # 家族树路由
│   ├── middleware/             # 中间件
│   │   ├── auth.js             # 认证中间件
│   │   └── upload.js           # 文件上传中间件
│   ├── scripts/                # 脚本
│   │   └── initDatabase.js     # 数据库初始化脚本
│   ├── uploads/                # 上传文件目录
│   ├── app.js                  # Express应用配置
│   ├── server.js               # 服务启动入口
│   ├── .env                    # 环境变量配置
│   └── package.json            # 依赖配置
│
└── frontend/                   # 前端 Vue3 + Element Plus
│   ├── src/
│   │   ├── api/                # API封装
│   │   │   ├── auth.js         # 认证API
│   │   │   ├── member.js       # 成员API
│   │   │   └── tree.js         # 家族树API
│   │   ├── views/              # 页面组件
│   │   │   ├── Login.vue       # 登录页
│   │   │   ├── Register.vue    # 注册页
│   │   │   ├── Home.vue        # 主页布局
│   │   │   ├── FamilyTree.vue  # 家族树视图
│   │   │   ├── MemberList.vue  # 成员列表
│   │   │   ├── MemberDetail.vue# 成员详情
│   │   │   ├── MemberEdit.vue  # 成员编辑
│   │   │   ├── Profile.vue     # 个人中心
│   │   │   └── UserManage.vue  # 用户管理
│   │   ├── components/         # 公共组件
│   │   │   ├── TreeNode.vue    # 树节点组件
│   │   │   └── MemberCard.vue  # 成员卡片组件
│   │   ├── store/              # 状态管理 (Pinia)
│   │   │   ├── auth.js         # 认证状态
│   │   │   └── member.js       # 成员状态
│   │   ├── router/             # 路由配置
│   │   │   └── index.js        # 路由定义
│   │   ├── utils/              # 工具函数
│   │   │   └── request.js      # Axios封装
│   │   ├── App.vue             # 根组件
│   │   └── main.js             # 入口文件
│   ├── vite.config.js          # Vite配置
│   └── package.json            # 依赖配置
│
└── 陈氏家族族谱方案设计.md      # 设计文档
```

---

## 六、启动命令

### 启动后端服务

```bash
cd D:\陈氏家族族谱_dev\backend
npm run dev
```

### 启动前端服务

```bash
cd D:\陈氏家族族谱_dev\frontend
npm run dev
```

### 初始化数据库（首次运行）

```bash
cd D:\陈氏家族族谱_dev\backend
npm run init-db
```

### 使用 ngrok 内网穿透（外网访问）

如需将本地服务暴露到公网供他人访问，可使用 ngrok 工具。

#### 1. 下载安装 ngrok

访问官网下载：https://ngrok.com/download

或使用命令安装：
```bash
# Windows (使用 Chocolatey)
choco install ngrok

# 或下载解压后添加到系统 PATH
```

#### 2. 注册并配置 Auth Token

1. 访问 https://dashboard.ngrok.com 注册账号
2. 获取 Authtoken
3. 配置 token：
```bash
ngrok config add-authtoken <your-authtoken>
```
#### your-authtoken： 3CqO2D28rwQ3MYAfsPkgqfDUE2L_3xWrrGYhGKCtq7cmfFrJZ

#### 3. 启动 ngrok 穿透

**方式一：分别穿透前后端服务**

```bash
# 穿透后端服务（端口 3000）
ngrok http 3000
# 新开终端，穿透前端服务（端口 5173）
ngrok http 5173
```

**方式二：使用配置文件同时穿透多个端口**

创建配置文件 `ngrok.yml`：
```yaml
version: "2.0"
tunnels:
  backend:
    proto: http
    addr: 3000
  frontend:
    proto: http
    addr: 5173
```

启动：
```bash
ngrok start --all
```

#### 4. 获取公网地址

启动后，ngrok 会显示类似以下信息：
```
Forwarding    https://xxxx-xxx-xxx-xxx.ngrok-free.app -> http://localhost:3000
Forwarding    https://yyyy-yyy-yyy-yyy.ngrok-free.app -> http://localhost:5173
```

- `https://xxxx...ngrok-free.app` 为后端 API 公网地址
- `https://yyyy...ngrok-free.app` 为前端页面公网地址

#### 5. 修改前端 API 地址

使用 ngrok 时，需要修改前端的 API 请求地址。

**方式一：修改环境变量文件（推荐）**

编辑文件 `frontend/.env`：
```env
# 将 localhost 地址改为 ngrok 后端地址
VITE_API_BASE_URL=https://你的后端ngrok地址/api
```

例如：
```env
VITE_API_BASE_URL=https://a1b2-c3d4-e5f6.ngrok-free.app/api
```

修改后需要**重启前端服务**：
```bash
# 停止当前前端服务，重新启动
cd D:\陈氏家族族谱_dev\frontend
npm run dev
```

**方式二：创建生产环境配置**

编辑文件 `frontend/.env.production`：
```env
VITE_API_BASE_URL=https://你的后端ngrok地址/api
```

然后构建前端：
```bash
cd D:\陈氏家族族谱_dev\frontend
npm run build
```

#### 6. 手机端访问配置

**重要说明**：
- 手机端访问时，前端和后端都必须使用 ngrok 公网地址
- 前端 ngrok 地址用于访问页面
- 后端 ngrok 地址用于 API 请求（在 `.env` 文件中配置）

**配置步骤**：

1. 启动后端 ngrok 穿透（端口 3000）：
```bash
ngrok http 3000
```
记录显示的公网地址，如：`https://xxxx.ngrok-free.app`

2. 修改前端配置 `frontend/.env`：
```env
VITE_API_BASE_URL=https://xxxx.ngrok-free.app/api
```

3. 重启前端服务

4. 启动前端 ngrok 穿透（端口 5173）：
```bash
ngrok http 5173
```
记录显示的公网地址，如：`https://yyyy.ngrok-free.app`

5. 手机浏览器访问：`https://yyyy.ngrok-free.app`

#### 7. 注意事项

- ngrok 免费版每次启动地址会变化，需重新配置
- 免费版有带宽和连接数限制
- 建议使用付费版获取固定域名
- 生产环境建议使用正式服务器部署
- **手机端访问必须同时穿透前端和后端两个端口**

---

## 七、数据库配置

数据库配置位于 `backend/.env` 文件：

```env
# 服务器配置
PORT=3000

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=cbj625629
DB_NAME=family_tree

# JWT配置
JWT_SECRET=chen_family_tree_secret_key_2024
JWT_EXPIRES_IN=7d
```

---

## 八、用户角色说明

| 角色 | 权限 |
|------|------|
| admin (管理员) | 所有权限，可管理用户和成员 |
| editor (编辑者) | 可添加、编辑家族成员信息 |
| viewer (查看者) | 仅可查看家族成员信息 |

---

## 九、API 接口列表

### 认证相关

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| GET | /api/auth/profile | 获取当前用户信息 |
| PUT | /api/auth/profile | 更新用户信息 |
| PUT | /api/auth/password | 修改密码 |
| GET | /api/auth/users | 获取所有用户（管理员） |
| PUT | /api/auth/users/:userId | 更新用户状态（管理员） |

### 成员管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/members | 获取成员列表 |
| GET | /api/members/:id | 获取成员详情 |
| POST | /api/members | 添加成员 |
| PUT | /api/members/:id | 更新成员 |
| DELETE | /api/members/:id | 删除成员 |
| GET | /api/members/:id/ancestors | 获取祖先链 |
| GET | /api/members/:id/descendants | 获取后代 |
| GET | /api/members/:id/relatives | 获取亲属关系 |
| POST | /api/members/:id/photo | 上传照片 |

### 家族树

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/tree | 获取家族树数据 |
| GET | /api/tree/generation/:gen | 获取指定代成员 |
| GET | /api/tree/stats | 获取统计数据 |
| GET | /api/tree/search | 搜索成员 |

---

## 十、技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Vue 3 |
| UI组件库 | Element Plus |
| 状态管理 | Pinia |
| 路由 | Vue Router 4 |
| HTTP客户端 | Axios |
| 构建工具 | Vite |
| 后端框架 | Node.js + Express |
| 数据库 | MySQL |
| 认证 | JWT (jsonwebtoken) |
| 密码加密 | bcryptjs |

---

## 十一、注意事项

1. **首次使用**：请先运行 `npm run init-db` 初始化数据库
2. **密码安全**：登录后请立即修改默认管理员密码
3. **用户审核**：新注册用户需要管理员审核通过才能登录
4. **文件上传**：照片上传支持 JPG、PNG、GIF、WEBP 格式，最大 5MB
5. **删除成员**：有子女的成员无法直接删除，需先处理子女关系

---

## 十二、后续扩展功能（规划）

- 移动端适配
- 家族大事记记录
- 家族相册共享
- 数据导出（PDF/Excel）
- 多家族支持
- GEDCOM格式导入导出

---

**开发完成日期**: 2026年4月25日